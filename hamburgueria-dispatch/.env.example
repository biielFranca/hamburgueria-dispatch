# Supabase
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# OSRM (roteamento) — opcional, usa servidor público por padrão
VITE_ROUTES_API_URL=https://router.project-osrm.org
VITE_OSRM_PROFILE=driving   # driving | bike | foot

# Tempo de espera para despachar pedido solo (padrão: 10 min)
# Em desenvolvimento, use 1 para facilitar testes
VITE_SOLO_WAIT_MIN=10

# Client IDs das plataformas (valores públicos, apenas identificação)
# NÃO colocar client_secret/service_key/webhook_secret aqui — são server-side.
# Secrets ficam em supabase/functions/.env (Edge Functions) e na tabela
# store_integrations (criptografada via pgsodium).
VITE_IFOOD_CLIENT_ID=
VITE_99FOOD_CLIENT_ID=
VITE_KEETA_CLIENT_ID=
